#!/bin/bash

if [ $# -eq 0 ]; then
    for a in `cleartool lsco -me -r -s -cview`; do cleartool diff -pred -ser $a; done
else
    for a in `cleartool lsco -me -r -s -cview $1`; do cleartool diff -pred -ser $a; done
fi
